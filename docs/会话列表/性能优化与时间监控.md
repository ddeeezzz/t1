# 性能优化与时间监控

## 本次会话记录（2026-04-17）

### 1. 用户诉求
- 用户要求：会话记录文件使用 `docs/会话列表/性能优化.md`。
- 用户要求：文档不需要过度详细，仅在用户明确要求“添加会话记录”时再追加。
- 用户要求：文档格式参考 `docs/会话列表/模块C真实流程.md`。
- 用户要求：后续讨论“分模块做时间统计和数据分析”的方案。

### 2. 本次会话实际动作
- 已将本文件从“详细时间线模板”调整为“会话记录模式”。
- 已按参考文档风格重写结构（用户诉求 / 实际动作 / 会话结果）。
- 已确认后续策略：默认不自动追加，仅在你明确指令后再记录。

### 3. 会话结果
- 文档已完成简化并对齐参考格式。
- 当前文件仅保留关键会话事实，不包含实现细节。
- 后续将按你的指令节奏追加记录。

---

## 增量会话记录（2026-04-17）

### 1. 用户新增现场日志
- 用户补充了模块 D 在双卡 A16 上的连续执行日志，并询问：
  - “这个速度 AnimateDiff A16 双卡正常吗”
- 用户日志包含关键提示：
  - `UNEXPECTED`：在“跨任务/跨架构加载”场景可忽略；若期望同构权重严格一致则不可忽略。
- 日志关键片段（摘录）：
  - `2026-04-17` AnimateDiff runtime 初始化完成（`device=cuda:1`）。
  - `shot_002~shot_011` 持续出现“帧数上限生效”（`inference_frames=16`，`exact_frames=43~131`）。
  - 多次进度条耗时接近：
    - `24/24 [03:17~03:19, 8.24~8.52s/it]`
  - 单元完成时间点呈双卡并行对称推进（如 `shot_002/003`、`004/005`、`006/007`、`008/009`、`010/011` 成对完成）。

### 2. 本轮速度复核事实（基于日志时间戳）
- 观测区间：`2026-04-17` 同日连续运行窗口。
- 单条 `shot`（16 推理帧）端到端耗时大致在：
  - `223s ~ 240s`（约 `3分43秒 ~ 4分00秒`）。
- 双卡整体吞吐（该窗口内）表现为：
  - 约每 `3分45秒` 完成 `2` 个 shot（并行成对推进，节奏稳定）。
- 进度条 `8.2~8.5s/it` 与单条端到端时长基本一致（含推理外开销如编码/调度切换）。

### 3. 当前结论（仅本轮现场）
- 对“`A16` 双卡 + AnimateDiff + `inference_frames=16` + `24 steps`”这一组合，
  - 当前速度属于“基本正常、略偏慢但可接受”的区间；
  - 如果对标 `4090/3090` 级别消费卡，会明显更慢，这在预期内。
- 就本段日志而言，未见异常抖动或退化征象：
  - 节奏稳定；
  - 成对并行稳定；
  - 没有出现单卡长期掉队或吞吐突然坍塌。

### 4. 会话结果
- 已按用户要求将本轮日志与速度判断写入本文件。
- 本轮结论可作为后续参数调优（步数/分辨率/并发窗口）基线参考。

---

## 增量会话记录（2026-04-18）

### 1. 用户确认实施方案
- 用户明确要求按既定提速方案直接落地实现，关键决策为：
  - `music_yby` 全量配置生效；
  - `steps: 24 -> 18`；
  - 分辨率 `848x480 -> 768x432`；
  - `num_frames` 保持 `16` 不变；
  - 在 `BC->D` 切换点做双卡 runtime 异步预热。

### 2. 本轮已落地变更（仅事实）
- 配置侧（仅 `configs/music_yby/*.json`）已批量更新：
  - `render.video_width=768`；
  - `render.video_height=432`；
  - `module_d.animatediff.steps=18`；
  - `module_d.animatediff.num_frames=16` 保持不变。
- 渲染侧新增 AnimateDiff runtime 预热入口：
  - `prewarm_animatediff_runtime(context, device_override)`；
  - 该入口仅触发 runtime 加载缓存，不执行推理。
- 调度侧新增 `BC->D` 异步预热逻辑：
  - 在 `d_device_pool` 解析后提交预热任务；
  - 预热与 D 派发并行，不阻塞首批 D 单元；
  - 设备预热“仅一次”去重；
  - BC 阶段已实际跑过 D 的设备在 D 阶段会跳过预热；
  - 预热失败仅记录 warning，不写链路失败、不阻断调度。

### 3. 测试与校验事实
- 已新增/补充测试覆盖：
  - 调度侧：D 阶段预热提交、BC 已热设备跳过、预热失败不阻断；
  - 渲染侧：预热入口仅调用 runtime 初始化，不触发推理。
- 本轮执行结果：
  - `./.venv/bin/pytest -q tests/test_cross_module_bcd_parallel.py tests/test_module_d_animatediff_renderer.py`
  - 结果：`21 passed`。
- 语法校验结果：
  - `python -m py_compile` 覆盖本轮改动文件，通过。

### 4. 会话结果
- 本轮方案已从“讨论阶段”进入“代码已落地 + 定向测试通过”状态。
- 后续可直接在 `music_yby` 任务上继续观测：
  - `shot p50` 时长变化；
  - `BC->D` 到双卡稳定派发的时间变化。
