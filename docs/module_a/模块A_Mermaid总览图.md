# 模块A Mermaid总览图

## 1. 说明
- 本文仅提供模块A链路 Mermaid 可视化图。
- 术语与命名遵循 `模块A_JSON产物输出链路总览.md` 中的统一命名口径（`A0/AL/B/M/S`、`BT*/ST*`）。

## 2. 总览图
```mermaid
flowchart TD
    I0[产物 输入上下文 task_id/audio_path/config]
    I1[产物 音频时长 duration_seconds]
    I2{产物 运行模式 mode}

    subgraph FB[规则链（fallback_only 或真实链回退）]
        F0[产物 规则大段 big_segments]
        F1[产物 规则小时戳 final_timestamps]
        F2[产物 规则小段 segments]
        F3[产物 规则节拍 beats]
        F4[产物 规则能量 energy_features]
        A0F[产物 analysis_data_fallback]
    end

    subgraph RL[真实链（real_*）]
        R0[产物 声轨分离结果 vocals.wav/no_vocals.wav]
        R1[产物 人声RMS序列]
        R2[产物 是否跳过FunASR]
        R3[产物 Allin1原始分析]
        R4[产物 A0段 stage1大段]
        R5[产物 Allin1节拍 beats]
        R6[产物 原始歌词单元 lyric_units_raw]
        R7[产物 清洗后句级歌词 sentence_lyric_units]
        R8[产物 分段锚点歌词 segmentation_anchor_lyric_units]
        R9[产物 AL段 歌词重算后大段]
        R10[产物 伴奏声学候选 onset+rms]
        R14A[产物 M0 人声阈值参数 vocal_threshold等]
        R14B[产物 M1 人声区间 vocal_ranges]
        R14[产物 M段 中段 mid_segments]
        R15A[产物 R1 人声歌词候选区间]
        R15B[产物 R2 器乐保护候选区间]
        R15C[产物 R3 长前置占位候选区间]
        R15[产物 R段 候选区间 range_items]
        R16A[产物 N1 短人声并合后区间]
        R16B[产物 N2 短器乐空档并合后区间]
        R16[产物 N段 并合归一区间 normalized_ranges]
        R11[产物 S段 最终小段]
        R12[产物 真实链能量特征]
        R13[产物 挂载后歌词单元 lyric_units]
        RCHK{产物 真实链完整性检查}
        A0R[产物 analysis_data_real]
    end

    AS[产物 analysis_data_selected]
    A1[产物 alias_map]
    O0[产物 module_a_output.json]

    I0 -->|探测音频时长，解决末段无法对齐总时长的问题| I1
    I1 -->|读取运行模式，解决同一入口下多链路调度的问题| I2

    I2 -->|进入规则链，解决模型失败或禁用时仍需产出的兜底问题| F0
    F0 -->|按固定间隔生成小时戳，解决无歌词无模型时的小段时间基线问题| F1
    F1 -->|按时戳构建小段，解决规则链对外segments字段完整性问题| F2
    F1 -->|按时戳映射节拍，解决规则链beats字段可用性问题| F3
    F2 -->|按规则RMS估算能量，解决规则链energy_features缺失问题| F4
    F0 -->|写入big_segments，解决规则链大段字段缺口问题| A0F
    F2 -->|写入segments，解决规则链小段字段缺口问题| A0F
    F3 -->|写入beats，解决规则链节拍字段缺口问题| A0F
    F4 -->|写入energy_features，解决规则链能量字段缺口问题| A0F

    I2 -->|进入真实链，解决需要高精度结构与歌词对齐的问题| R0
    R0 -->|提取人声RMS做预检，解决纯器乐仍强跑识词的耗时问题| R1
    R1 -->|低能量判定是否跳过FunASR，解决无效识词和误报问题| R2
    R0 -->|执行Allin1结构分析，解决大段与节拍主来源问题| R3
    R3 -->|段落解析与归一化，解决原始标签与内部契约不一致问题| R4
    R3 -->|节拍契约映射，解决Allin1输出到模块A beats字段的格式适配问题| R5

    R2 -->|不跳过时识别歌词，解决人声段缺少文本与时间锚点的问题| R6
    R2 -->|跳过时输出空歌词，解决低能量场景下误识词污染分段的问题| R6
    R6 -->|歌词清洗，解决未识别噪声词和异常边界的问题| R7
    R7 -->|生成分段锚点（只产证据不切段），解决后续切句缺少歌词证据的问题| R8

    R4 -->|以A0为基线重算大段，解决仅靠结构标签时边界不贴歌词的问题| R9
    R8 -->|注入歌词锚点证据，解决大段边界与句首句尾错位问题| R9

    R0 -->|提取伴奏onset和RMS候选，解决器乐段无歌词时的节奏切分依据问题| R10
    R9 -->|进入M0分支门控，解决器乐与人声共用同一切分策略的误切问题| R14A
    R1 -->|输入人声RMS并计算阈值，解决不同音量歌曲阈值不自适应的问题| R14A
    R14A -->|支路一：命中器乐标签集合时直接写入M段并标记inst，解决器乐段被误送入人声阈值的问题| R14
    R14A -->|支路二：仅非器乐标签进入M1阈值切分，解决真实人声段需要细切的问题| R14B
    R14B -->|峰值低于或接近阈值时回落为inst并写入M段，解决allin1的人声段出现大段纯器乐的问题| R14
    R14B -->|峰值明显高于阈值时切出vocal_ranges并做短静音并合，解决人声连续区与静音区混杂问题| R14

    R14 -->|人声中段按已生成锚点执行切句，解决一句歌词跨多拍或喘息抢拍的问题| R15A
    R14 -->|器乐中段做边界歌词保护，解决切点压到歌词首尾的问题| R15B
    R14 -->|器乐长段单次切分，解决长器乐段画面停滞的问题| R15B
    R15A -->|汇入人声候选，解决人声路径候选缺失问题| R15
    R15B -->|汇入器乐候选，解决器乐路径候选缺失问题| R15
    R15A -->|沉淀长前置占位段（句间超长空档占位段），解决超长前置空档无独立占位的问题| R15C
    R15C -->|并入总候选池，解决长空档在总时间轴中丢失问题| R15
    R10 -->|提供伴奏节奏候选，解决无歌词区切点不足问题| R15
    R8 -->|提供歌词锚点约束，解决切点偏离句首句尾问题| R15

    R15 -->|N1短人声并合，解决无歌词短句碎片过多导致频繁切镜问题| R16A
    R16A -->|N2短器乐空档并合，解决人声之间短空挡被单独成段的问题| R16B
    R16B -->|时间轴归一，解决首尾不对齐和段间缝隙重叠问题| R16
    R16 -->|落成最终小段，解决内部候选无法直接对外输出的问题| R11

    R11 -->|按RMS与节拍计算能量，解决下游镜头强弱缺少量化依据的问题| R12
    R8 -->|歌词侧输入挂载，解决歌词仅有句级未绑定片段的问题| R13
    R11 -->|段落侧输入挂载，解决歌词未映射到segment时间窗的问题| R13

    R9 -->|提交候选大段，解决真实链大段字段完整性校验问题| RCHK
    R11 -->|提交候选小段，解决真实链小段字段完整性校验问题| RCHK
    R5 -->|提交候选节拍，解决真实链beats字段完整性校验问题| RCHK
    R12 -->|提交候选能量，解决真实链能量字段完整性校验问题| RCHK
    R13 -->|提交候选歌词，解决真实链歌词字段完整性校验问题| RCHK
    RCHK -->|通过则保留真实链，解决优先高质量结果输出问题| A0R
    RCHK -->|失败则触发规则回退，解决真实链中断导致无输出问题| F0

    A0R -->|选择真实链结果，解决真实链可用时质量优先问题| AS
    A0F -->|选择规则链结果，解决真实链失败时可用性兜底问题| AS

    AS -->|生成命名映射，解决不同命名视图共存的可读性问题| A1
    A1 -->|契约校验并落盘，解决字段缺失或非法结果外泄问题| O0
```

## 3. 图例（缩写速查）
- `A0段`：Allin1 直出大段（stage1）。
- `AL段`：按歌词证据重算后的大段。
- `M段`：中段（按人声能量先切出的内部段，不直接对外）。
- `R段`：候选区间（歌词优先与器乐保护后的候选切分结果）。
- `N段`：并合归一区间（短段并合与时间轴归一后的内部结果）。
- `S段`：最终对外小段（`segments`）。
- `长前置占位段`：人声路径里两句歌词之间 gap 过长时，从前一句结束到后一句开始单独拆出的占位区间（`preserve_gap_segment=true`），用于避免超长空档被吞并到歌词段内。
- `BT* / ST*`：属于 `alias_map` 的时间戳命名视图，本图为突出主产物链路已省略显示。
- `analysis_data`：模块A中间汇总结构（大段/小段/beats/歌词/能量）。

## 4. M段SOP（判断标准）
1. `AL段 -> M0`：先按大段标签做“门控分支”。  
器乐标签命中（`intro/inst/outro/...`）直接落到 `M段(inst)`，不进入人声阈值切分；只有非器乐标签才进入 `M1` 人声RMS判定。
2. `M0` 阈值计算：  
静音地板 `floor` 由人声RMS低分位估计并夹紧到 `[0.0015, 0.02]`；  
人声阈值 `vocal_threshold = min(0.08, floor + max(0.0008, floor*0.25))`。
3. `M0 -> M1`：在每个非器乐大段内，用单阈值提取 `vocal_ranges`。  
若整段峰值不高于 `vocal_threshold`，该段回落为器乐中段。  
4. `M1 -> M段`：对已切出的人声区间做平滑与最小时长过滤。  
`min_vocal = max(0.06, mid_segment_min_duration*0.15)`；  
`min_silence = max(0.08, mid_segment_min_duration*0.35)`；  
并合相邻同角色毛刺段后，将结果写回 `M段`（既可能有人声子段，也可能有器乐子段）。
5. `M段 -> R1`（人声路径）：  
按歌词锚点切句；句间 gap 处理为：`<=5.0s` 前并到前一句末尾，`>5.0s` 拆出独立占位段（保留标记 `preserve_gap_segment`）。  
职责边界说明：`R7 -> R8` 负责“生成锚点证据”，`M段 -> R1` 负责“消费锚点并真正切句”，两步不是重复关系。
6. `M段 -> R2`（器乐路径）：  
先做边界歌词保护（窗口 `0.35s`、最小微段 `0.06s`）；  
对器乐长段仅单次切分：时长 `<4.0s` 不切，`>=4.0s` 才切，候选优先 `onset`，无 onset 才用 `beat`，切点选能量突变最大。
7. `R段 -> N1 -> N2`：  
先并合短人声无歌词段（阈值默认 `1.2s`）；  
再并合人声之间短器乐空挡（跨组 `1.2s`，同组 `1.4s`）；  
最后统一时间轴连续性，输出 `N段`。
8. `N段 -> S段`：  
生成最终 `segments`（`segment_id/start/end/label/big_segment_id`），首段对齐 `0`、末段对齐总时长。
